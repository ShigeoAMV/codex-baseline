Production requests must no longer wait forever. In `config/service.json`, set
`requestTimeoutMs` to 7500 milliseconds. Preserve every other setting, key,
value, and value type exactly. Do not create files or change retry or telemetry
behaviour.
