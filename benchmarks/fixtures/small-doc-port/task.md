Correct the two stale port facts in `README.md` so they agree with the shipped
`server.js`: use the supported environment variable in the launch example and
state the real default port. Keep the health-check example and all other prose.
Do not modify the implementation or add files.
