# AUTO execution overlay (evaluation only)

This profile is a trusted input to isolated live evaluation. It is shipped only
as an inert benchmark artifact: installation never appends it to active global
guidance, and the harness never copies it into repository or user task text.

For this evaluation only, lift the release-candidate SOLO activation gate and
apply the installed autonomous SOLO/TEAM/SWARM selection and safety rules. This
does not establish production eligibility. Record planned decisions separately
from observed runtime receipts, and record unavailable observations as
`unverified`/`null`; never estimate them.
