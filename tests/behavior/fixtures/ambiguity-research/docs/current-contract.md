# Current API contract

Clients currently consume `legacy_name`. The repository does not state whether
the old field may be removed immediately or must remain during a deprecation
window. No compatibility duration has been selected.
