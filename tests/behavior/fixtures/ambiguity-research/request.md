# Public-field rename

Plan a large rename of the public response field `legacy_name` to `display_name`.
Research the supplied repository first. Do not modify files. If an important
choice is genuinely absent, ask only that material decision rather than asking
for facts already documented here.
