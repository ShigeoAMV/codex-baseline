# v0.3.0-rc.1 final local verification

Date: 2026-08-16

This receipt covers the uncommitted `0.3.0-rc.1` working-tree snapshot. It is
local executable evidence, not an immutable revision, signed attestation,
stable release, or performance claim.

## Payload

- Installable files: 76
- Release status: `rc.1`
- Final payload SHA-256:
  `8356e50d0f4321d7f2038816866e019f0a94a6fc23123bdda10c3025de50cfcd`

## Unix/WSL matrix

The pre-final-security-delta working tree (payload `5afc214d...0902`) was copied
over a real private ext4 Git clone and run
with the repository's full entry point:

```text
bash ./tests/run.sh
```

Result: exit `0`, all 15 TAP groups passed. The matrix includes lifecycle,
crash recovery, journal/path adversaries, real ACL/xattr checks, onboarding,
optimizer, benchmark containment and truth contracts, routing/behavior,
worktree isolation, cross-version self-update, docs, and prompt discovery.
The emitted `Killed` lines are deliberate fault injections whose recovery
assertions passed.

Local retained log for this run:
`/home/andrin/.cache/codex-baseline-runs/final-v03-git.GS0HCa/full.log`.

The final payload removes informational lifecycle Git calls to prevent
repository-local filter/helper execution. Its focused private-ext4 lifecycle
group passed with exit `0`, including a hostile `git` shim that must not execute.

## Native Windows matrix

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\lifecycle.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\windows\onboard-benchmark.ps1
```

Results:

- exit `0`: Windows lifecycle, 604 assertions on Windows PowerShell
  5.1.26100.8875, including PowerShell 7.6.3 compatibility cases;
- exit `0`: Windows onboarding/benchmark, 79 assertions.

Those full Windows counts cover the `5afc214d...0902` snapshot. The final
Git-free provenance delta additionally passed a focused native test with 13
assertions across PowerShell 5.1 and PowerShell 7.6.3; hostile `git.cmd` shims
did not execute and revision/dirty output remained explicitly unavailable.

## Deliberately unavailable evidence

No live autonomous performance result is claimed. Codex CLI 0.147 does not
expose authoritative child lane, requested/actual child model and effort,
depth, wave, concurrency, or parent-setting telemetry through its JSONL
contract. The RC also has no independently distributed runner-attestation
trust root. Consequently stable promotion is unavailable; a benchmark API key
alone cannot authorize it.

## Fresh review

A fresh combined read-only security, architecture/maintainability, and
conformance review found one High in informational lifecycle Git provenance.
After lifecycle Git invocation was removed, the same reviewer performed a
bounded causal delta review and returned **Critical 0 / High 0 — GO**. The
working tree remained shared rather than platform-enforced read-only, but the
reviewer performed no writes or network access.
